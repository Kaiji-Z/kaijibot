import { describe, expect, it } from "vitest";

describe("secrets runtime snapshot nextcloud talk file precedence", () => {
  it("skips nextcloud-talk tests — extension removed in this fork", () => {
    expect(true).toBe(true);
  });
});
