---
summary: "CLI reference for `kaijibot clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `kaijibot clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `kaijibot clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `kaijibot clawbot qr` (same behavior as [`kaijibot qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `kaijibot clawbot qr` -> `kaijibot qr`
